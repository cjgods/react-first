1.自定义组件必须大写开头，原生组件小写
2.原生标签绑定事件必须驼峰  onClick
3.事件传参和获取事件this吗，需要使用bind,第一个参数必须是this
4.setState 是异步的，不会立马改变值。是在队列中
5.setState参数为函数的话，可以获取到上次立马返回的值
6.子组件使用this.props获取参数，自定义组件传参可以传递任意类型数据
7.组件可以使用defaultProps配置props的默认属性
8.this.prop里面的值不能被直接修改，需要使用setState修改其中的值
9.state 是让组件控制自己的状态，props 是让外部对组件自己进行配置

